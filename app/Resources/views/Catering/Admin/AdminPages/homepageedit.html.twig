{% extends "Catering/Admin/adminindex.html.twig" %}
{% block userdata %}
    {% for aboutdata in aboutcompany %}
    <div class="admin-edition-form-container auto-side-margins">
        <div class="header"><a href="{{ url("homepage_description_details", {"id": aboutdata.aboutid}) }}">{{ aboutdata.abouttitle }}</a></div>
        <div class="description">{{ aboutdata.aboutdescription }}</div>
    </div>
        <div class=" page-dotted-separator auto-side-margins" style="margin-top: 30px; margin-bottom:30px"></div>
        {% endfor %}
    <a href="{{ url("homepage_description_create") }}" class="button align-center" style="width:150px; color:black; font-size:14px" >Dodaj zawartość</a>
{% endblock %}