{% extends "Catering/Admin/adminindex.html.twig" %}
{% block userdata %}
    <div class="admin-offerpage-container">
        <div class="desc-container">
            {{ form_start(form) }}
            {{ form_label(form.title) }}
            {{ form_widget(form.title) }}
            {{ form_label(form.description) }}
            {{ form_widget(form.description) }}
            {{ form_widget(form.submit, {"attr": {"class": "button", "style": "float:left"}}) }}
            <a href="{{ url("admin_edit_offerpage") }}" class="button align-center" style="display:inline">Powrót</a>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}